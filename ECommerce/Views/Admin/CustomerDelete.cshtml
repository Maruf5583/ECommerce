@model Customer




<div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8">

        <h2 class="text-danger my-4">Delete Customer</h2>
        <p class="text-muted">Are you sure you want to delete this customer?</p>


        <form method="post" action="/Admin/CustomerDelete/@Model.Id">

            <div class="text-center my-4">
                @if (!string.IsNullOrEmpty(Model.Image))
                {
                    <img src="~/CustomerImage/@Model.Image"
                         width="250" height="250"
                         class="img-thumbnail rounded-circle" />
                }

                  

                <div class="form-control my-3 bg-light">
                    <strong>Name:</strong> @Model.Name
                </div>
                <div class="form-control my-3 bg-light">
                    <strong>Email Address:</strong> @Model.Eamil
                </div>
                <div class="form-control my-3 bg-light">
                    <strong>Phone Number:</strong> @Model.Phone
                </div>
                <div class="form-control my-3 bg-light">
                    <strong>Password:</strong> @Model.Password
                </div>
                <div class="form-control my-3 bg-light">
                    <strong>Gender:</strong> @Model.Gender
                </div>
                <div class="form-control my-3 bg-light">
                    <strong>Country:</strong> @Model.Country
                </div>
                <div class="form-control my-3 bg-light">
                    <strong>City:</strong> @Model.City
                </div>
                <div class="form-control my-3 bg-light">
                    <strong>Address:</strong> @Model.Address
                </div>

                    

            </div>
            <button type="submit" class="btn btn-danger my-3">Delete</button>
            <a asp-action="FetchCustomer" asp-controller="Admin" class="btn btn-secondary my-3">Back to List</a>

        </form>
                


